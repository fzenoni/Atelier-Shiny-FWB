library(shiny)
library(bslib)

# Define UI ----
ui <- page_fluid(

  # Écrivez le code ici

)

# Define server (empty) ----
server <- function(input, output, session) {
}

# Create Shiny app ----
shinyApp(ui, server)
