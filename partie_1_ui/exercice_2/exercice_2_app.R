library(shiny)

# Define UI ----
ui <- fluidPage(

  # Insérez le code ici

)

# Define server (empty) ----
server <- function(input, output, session) {
}

# Create Shiny app ----
shinyApp(ui, server)
